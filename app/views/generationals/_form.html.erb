<%= form_with(model: generational) do |form| %>
  <% if generational.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(generational.errors.count, "error") %> prohibited this generational from being saved:</h2>

      <ul>
        <% generational.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
